#pragma once
#include <vector>
#include <string>

//leetcode.com/problems/generate-parentheses/description/
std::vector<std::string> generate_parantheses(int n)
{
    std::vector<std::string> ret;
    int nOpen(n), nClosed(n);

    return ret;
}